require "rails_helper"

feature "user can create a park" do
  pending "signed in user can create a park"
  # pending "admin can create a park?" 
  pending "non-signed in user cannot create a park"
end
