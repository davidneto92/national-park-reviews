<h1>All the parks!</h1>

<div class="index-display">
  <ul>
    <% @parks.each do |park| %>
      <li>
        <span class ="park-entry">

          <div class="park-picture-wrapper">
            <a href="<%= "parks/#{park.id}" %>">
            <%= image_tag park.main_image.thumb %></a>
          </div>

          <div class="text-wrapper">
            <%= link_to "#{park.name}", park_path(park) %>
            <%= park.state %>
          </div>

        </span>
      </li>
    <% end %>
  </ul>
</div>
